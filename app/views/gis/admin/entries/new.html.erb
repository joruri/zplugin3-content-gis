<%= form_for @item, as: :item, url: { action: :create }, :html => {:method => :post, :multipart => true} do |f| %>
  <%= error_messages_for :item %>
  <%= render 'form', f: f %>
  <%= submit @content.submit_state_options(Core.user) %>
<% end %>
